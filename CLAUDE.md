# Project Structure

- **Backend**: `src/backend/PocNexus/` (.NET 10, solution file: `PocNexus.sln` at repo root)
- **Frontend**: `src/frontend/` (Angular)

## Build Commands (run from repo root)

```bash
dotnet build
```
